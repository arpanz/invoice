# 🧾 Invoice Maker Pro

> **"Generate professional, GST-ready invoices in 60 seconds. 100% Offline. 100% Private."**

A premium Flutter invoice app that feels like enterprise software but fits in your pocket. Built with a Feature-First architecture for maximum scalability and reusability.

---

## 📱 Screenshots

| Dashboard | Create Invoice | Invoice History | Settings |
|-----------|---------------|-----------------|----------|
| Outstanding balance, metrics, recent invoices | Single-scroll form with live grand total | Swipe to mark paid / delete | Pro upsell, business profile, preferences |

---

## 💰 Monetization

### Free Tier
- ✅ Unlimited invoices
- ✅ All GST/Tax features
- ⚠️ Subtle watermark: *"Generated by Invoice Maker Pro"*
- ⚠️ Cannot upload custom business logo
- 📢 Banner ad on Dashboard + Interstitial after PDF export

### Pro Tier — **$4.99 / ₹299 One-Time IAP**
- 🚫 Removes watermark
- 🎨 Unlocks custom logo upload
- 🚫 Removes all ads

---

## 🏗️ Architecture

Feature-First architecture with a reusable `core/` layer:

```
lib/
├── core/                    # ♻️ Reusable across apps
│   ├── ads/                 # AdMob banner + interstitial
│   ├── billing/             # IAP service ($4.99 one-time)
│   ├── database/            # SQLite via sqflite
│   ├── theme/               # Deep Blue (#2563EB) + Inter font
│   └── utils/               # Currency formatter, PDF helper
│
├── features/
│   ├── dashboard/           # Overview: outstanding, metrics, recent
│   ├── invoices/            # Create, history, PDF generator
│   ├── clients/             # Client CRUD with search
│   └── settings/            # Business profile, preferences, paywall
│
├── shared_widgets/          # CustomTextField, PrimaryButton, EmptyState
└── main.dart                # 4-tab navigation shell
```

---

## 🛠️ Tech Stack

| Category | Package |
|----------|---------|
| State Management | `provider` |
| Database | `sqflite` (relational: Client → many Invoices) |
| PDF Generation | `pdf` + `printing` |
| File Handling | `path_provider` + `share_plus` |
| UI | `google_fonts` (Inter) + `intl` |
| Monetization | `google_mobile_ads` + `in_app_purchase` |
| Image Picker | `image_picker` (Pro logo upload) |

---

## 📑 PDF Features

- **Header**: Logo (Pro) or initials avatar + "INVOICE" title + invoice number
- **Addresses**: FROM (your business) and BILL TO (client) side-by-side
- **Line Items Table**: Clean dark header, alternating row colors
- **Totals**: Subtotal → Discount → SGST/CGST/IGST → Grand Total (blue footer)
- **Footer**: Bank details + "Thank you for your business!" + watermark (free tier)

---

## 🚀 Getting Started

### Prerequisites
- Flutter SDK ≥ 3.5.0
- Android Studio / Xcode

### Setup

```bash
# Install dependencies
flutter pub get

# Run on device/emulator
flutter run

# Build release APK
flutter build apk --release
```

### Before Publishing

1. **AdMob**: Replace test IDs in [`lib/core/ads/ad_manager.dart`](lib/core/ads/ad_manager.dart) and [`android/app/src/main/AndroidManifest.xml`](android/app/src/main/AndroidManifest.xml)
2. **IAP**: Set up `invoice_maker_pro_lifetime` product in Google Play Console / App Store Connect
3. **App Name**: Update `android:label` in AndroidManifest and iOS `Info.plist`

---

## 📂 Key Files

| File | Purpose |
|------|---------|
| [`lib/features/invoices/services/pdf_generator_service.dart`](lib/features/invoices/services/pdf_generator_service.dart) | The PDF drawing engine |
| [`lib/core/database/db_provider.dart`](lib/core/database/db_provider.dart) | SQLite schema + CRUD helpers |
| [`lib/core/billing/billing_service.dart`](lib/core/billing/billing_service.dart) | IAP purchase + restore logic |
| [`lib/features/settings/widgets/paywall_bottom_sheet.dart`](lib/features/settings/widgets/paywall_bottom_sheet.dart) | Pro upsell UI |
| [`lib/main.dart`](lib/main.dart) | App entry point + 4-tab navigation |

---

## 📄 License

Private — All rights reserved.
